defaults:
  - common:
      - drivable_area_violation_statistics
      - ego_lane_change_statistics
      - ego_progress_along_expert_route_statistics
      - ego_relative_progress_to_goal_statistics
      - ego_jerk_statistics
      - ego_lat_acceleration_statistics
      - ego_lon_acceleration_statistics
      - ego_lon_jerk_statistics
      - ego_yaw_acceleration_statistics
      - ego_yaw_rate_statistics
  - high_level_common:  # High level common metric that depends on lower level metrics, they can also rely on the previously called high level metrics
      - ego_at_fault_collisions_statistics
      - time_to_collision_statistics
      - speed_limit_violation_statistics
      - ego_is_comfortable_statistics
