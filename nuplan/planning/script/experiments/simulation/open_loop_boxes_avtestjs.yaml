# @package _global_
experiment_name: simulation_open_loop_boxes_avtestjs

defaults:
  - override /observation: box_observation
  - override /ego_controller: log_play_back_controller
  - override /planner: log_future_planner

  - override /callback:
      - simulation_log_callback
      - serialization_callback
